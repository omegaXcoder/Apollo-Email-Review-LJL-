# Copy of the .env template (safe to commit — contains no secrets).
# Fill in the real values in .env, not here.
# APOLLO_SEQUENCE_IDS and REPORT_EMAILS accept comma-separated lists.

APOLLO_API_KEY=
APOLLO_SEQUENCE_IDS=auto
ANTHROPIC_API_KEY=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=omega@kudos.marketing
SMTP_PASS=
REPORT_EMAILS=omega@kudos.marketing
AUTO_APPLY=true
